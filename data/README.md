Shared dataset directory for both `ir_system` and `ie_system`.

- `skills_data.json`: current main dataset shared by both systems
- `skills_data.csv`: CSV export of the main dataset
- `skills_checkpoint.json`: crawler checkpoint and crawl state
- `skills_data_legacy_100.json`: older 100-record snapshot kept only for reference
